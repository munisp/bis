// BIS Standalone PWA — tRPC stub
// The standalone BIS PWA uses a mock data layer instead of a live tRPC server.
// All pages that were copied from TourismPay and reference trpc are being
// rewritten to use the mockData layer. This stub prevents import errors during
// the transition.

export const trpc = {} as any;
